atlas scan (depth>=3) after ingest reused low FEAT-011..015 ids -> duplicate node id + stale scan tag; check FAIL. Repair: delete dup ids (force wipes referencing conns) then re-record; rebuild to fix words_index.
