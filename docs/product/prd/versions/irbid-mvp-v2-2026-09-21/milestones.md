# Milestones: irbid-mvp-v2-2026-09-21

Status: REVIEWED. Aktif sejak 2026-09-22 (`irbid-mvp-v2-2026-09-21`). Mulai dari M0.

Target: repo ini (`sa7tein-pwa`) — 4 shell role sudah dibangun (customer, merchant, courier, panel admin/CS) + **Super Admin sebagai role terpisah** yang belum dibangun dan kelak berupa website penuh non-PWA (keputusan PO 2026-09-23). Referensi UI/UX 4 portal (termasuk portal CS) ada di `repo-sa7tein-schema.md`, tapi bukan target.

| ID | Requirement IDs | Dependencies | Owner | Acceptance criteria | Mock state | BE contract | Status |
|---|---|---|---|---|---|---|---|
| M0 | R-FEE-01, R-WALLET-01, R-COD-01, R-CURR-01, R-TOPUP-01, R-LEDGER-01 | — | FE | `src/types.ts` mengekspor `Wallet`, `TopUp`, `Payout`, `ExchangeRate`; `PaymentMethodId` mencakup `wallet`, `xendit_va`, `xendit_qris`; konstanta `PLATFORM_FEE_MERCHANT_JOD`, `PLATFORM_FEE_CUSTOMER_JOD`, `MIN_TOPUP_NEW_ACCOUNT_JOD` ada di `src/data/merchant.ts`; `src/store/slices/walletSlice.ts` dibuat (balance, topUpHistory, payoutHistory) dan terdaftar di `src/store/index.ts` combineReducers + whitelist persist | Tipe compile; walletSlice tampil di Redux DevTools; konstanta bisa diimpor | Wallet{balance,available,pending}, TopUp{amount,channel,status}, Payout{amount,status}, ExchangeRate{base,quote,rate,fetched_at}. Ledger = double-entry append-only (backend only). | done |
| M1 | R-CURR-01 | M0 | FE | Harga IDR tetap source of truth; setiap nominal dirender dengan padanan JOD dari rate mock; widget kurs menampilkan waktu sync terakhir; disclaimer "kurs estimasi, mengikuti kurs harian" tampil | Rate mock di data/store (mis. 1 JOD = Rp23.000); komponen harga menampilkan IDR + JOD | `exchange_rates{base,quote,rate,fetched_at,source}`; sync 1×24 jam dari API eksternal; fallback pakai rate cache terakhir | done |
| M2 | R-FEE-01, R-TOPUP-01 | M0 | FE | Checkout menampilkan breakdown: subtotal + ongkir + fee customer 0,22 JOD + total; selector wallet tampil (wallet/cod/transfer); gate memblokir checkout kalau saldo akun baru < 3,5 JOD (pesan gate) | cartSlice `selectedPaymentId`; saldo < 3,5 JOD memunculkan gate; checkout render baris fee dari konstanta | Order divalidasi `wallet_balance >= MIN_TOPUP_NEW_ACCOUNT_JOD`; `fee_customer_jod = 0.22`, `fee_merchant_jod = 0.15` tercatat per order | done |
| M3 | R-WALLET-01, R-TOPUP-01, R-CASHOUT-01 | M0, M2 | FE | Tiga layar wallet ada: `WalletBalance` (available + pending), `WalletTopUp` (mock Xendit VA/QRIS), `WalletPayout` (riwayat payout, fee placeholder bila OQ-22 belum final); rute terdaftar di `src/App.tsx` | walletSlice punya `topUpHistory` + `payoutHistory` mock; layar render dalam shell 430px | `POST /topups` (channel, amount) → redirect Xendit VA/QRIS; `POST /v3/payouts` (amount, bank) → webhook `v3_payout.*`; idempotency-key wajib | done |
| M4 | R-COD-01 | M0, M2 | FE | Alur COD via wallet demo: order dibuat → hold tampil → setelah aksi mock "kurir match" → cut tampil → setelah OTP → settled. Batal sebelum match = "hold released"; sesudah match = "hold reversed" | cartSlice punya branch hold/cut/settle; transisi terlihat di detail order | `order.hold_status` (`none/held/cut/settled/released/reversed`); event `hold_created`, `hold_cut`, `hold_settled`, `hold_released`, `hold_reversed`; tiap transisi = 1 entry ledger append-only | done |
| M5 | R-DELIV-01 | M0, M4 | FE | `OrderStageScreen` punya tombol checkpoint: "Ambil" (konfirmasi merchant 2-way), "Berangkat" (mulai timer), "Tiba" (geolocation + foto mock), "Selesai" (input OTP); timer SLA 15/30/10 tampil sebagai hitung mundur; indikator auto-settle muncul 10 menit setelah "Tiba" | OrderStageScreen diperluas dengan checkpoint + timer; state timer di cartSlice atau komponen lokal; OTP mock 4 digit | `checkpoint{type,timestamp,evidence}`; `timer{checkpoint,start,duration}`; `otp{code,verifiedAt}`; auto-settle 10 menit; GPS snapshot sekali saat "Tiba" | done |
| M6 | R-DISPUTE-01 | M0, M4, M5 | FE | Customer/merchant bisa "Ajukan Sengketa" dari detail order (kategori + alasan + foto mock), 1× per order, window 24 jam; order jadi `disputed` dan hold dibekukan; Super Admin punya queue dispute dengan 4 tombol resolusi; status menampilkan `resolved_*` | `DisputeSubmit.tsx` + `SuperAdminQueue.tsx` + `disputeSlice.ts`; queue menampilkan order + bukti delivery + ledger mock; auto-settle pause saat disputed | `order.disputed`, `order.resolved_{refund_full\|refund_partial\|released\|rejected}`; event `dispute_filed`, `dispute_resolved`; tiap resolusi = 1 entry ledger append-only; kasus tanpa pihak bersalah dari `protection_fund` | done |
| M7 | R-TAX-01 | M0, M2 | FE | Checkout menampilkan 2 baris pajak: GST makanan (Jordan 16%, info "merchant setor") dan GST fee platform (info "kewajiban platform"), keduanya info-only di MVP | Konstanta pajak mock; dua baris tampil sebagai info (disabled) | `gst_amount` (lapis 1, merchant) + `platform_gst` (lapis 2, platform); status belum final — konsultan pajak | done |
| M8 | R-PUSH-01 | M0 | FE | Registrasi mewajibkan nomor WA + validasi E.164 (`+962`/`+62`); `NotificationSettings` punya pendaftaran push subscription (mock); tombol fallback WA `wa.me` tampil saat lawan offline | Subscription mock di store; field nomor WA tervalidasi; tombol WA membuka deep link | `subscription{endpoint,keys,platform,expiresAt}`; push payload ≤4KB, TTL wajib, `userVisibleOnly`; fallback `https://wa.me/<E164>?text=` | done |
| M9 | R-LEDGER-01 | M0, M3, M4, M6 | FE | Layar Super Admin menampilkan saldo wallet agregat, total liability, saldo Xendit mock, dan flag kalau saldo Xendit < total liability; ledger menampilkan daftar entry append-only tanpa aksi edit/hapus | Ledger mock di store; metrik liability tampil; entry tidak bisa diubah dari UI | `ledger_entries` append-only; reversal via entry baru; rekonsiliasi harian Xendit vs ledger | done |
| M10 | R-INCENTIVE-01 | M0, M3, M9 | FE | Merchant baru otomatis dapat `merchant_credit` 5 JOD (non-withdrawal) dan fee 0,15 JOD per order dipotong dari credit itu sampai habis (~33 order); layar merchant menampilkan sisa modal + progress tier bulan berjalan; saat ambang tier terlewati (500/1.000/1.250 order settled dalam periode), UI menandai tier tercapai dan menampilkan cashback 15/40/62,5 JOD | `merchantSlice.credit` punya `merchant_credit`, `rebate_tier`, `rebate_period` (dipindah dari `walletSlice` — lihat docs bagian 31); data order mock cukup untuk melewati ambang Tier 1; riwayat cashback tampil | Field: `merchant_credit_balance`, `rebate_tier`, `rebate_period` (YYYY-MM), `rebate_amount_jod`, `rebate_paid_at`. Events: `merchant_credit_granted`, `merchant_credit_debited`, `rebate_tier_reached`, `rebate_paid`. Modal non-withdrawal; cashback masuk dompet deposit merchant (withdrawability UNRESOLVED I-3). I-1..I-6 belum final | done |
| M11 | semua R-* | M0–M10 | FE | Journey lintas role konsisten: order yang sama tampil konsisten di customer, merchant, courier, Super Admin; status + hold + dispute + saldo/credit sinkron; ukur 390px & 1440px: kolom 430px, tepi 20px, overflow horizontal 0; halaman `/documentation` diperbarui | Semua layar render di kedua breakpoint; Documentation.tsx diperbarui | Semua kontrak BE M0–M10 dikonsolidasi; daftar endpoint, event, state machine, pemilik validasi | done |

## Notes

- **Urutan dependensi**: M0 (tipe + kontrak) → M1/M2 (currency + fee) → M3 (wallet) → M4 (COD hold) → M5 (delivery + OTP) → M6 (dispute) → M7 (pajak) → M8 (push) → M9 (ledger) → M10 (insentif merchant) → M11 (verifikasi lintas role).
- **Status per 2026-09-23**: **M0–M11 `done`** — tipe/konstanta/walletSlice, tampilan IDR+JOD, fee checkout + gate saldo, wallet & top-up/payout, hold COD, checkpoint pengiriman + OTP, sengketa, pajak info-only, nomor WA + push mock, ledger & liability, insentif merchant, dan verifikasi lintas role. Sisa pekerjaan ada di bagian "Item di luar milestone" di bawah.
- **Repo ini mock-only**: seluruh kolom kontrak BE menjelaskan API masa depan. Tidak ada backend, pembayaran, atau auth sungguhan (AGENTS.md §1).
- **Panel admin (CS) vs Super Admin**: konsol `/admin` adalah panel admin platform yang dikelola **CS** (keputusan PO 2026-09-23); shell 430px dipertahankan karena CS memakainya dari ponsel. **Super Admin role terpisah** — kelak website penuh non-PWA (prefix `/superadmin`), cakupan `UNRESOLVED` (lihat `decision-irbid-mvp.md`). Berlaku untuk M6 dan M9: aktornya panel CS, bukan Super Admin.
- **Referensi UI/UX**: repo 4 portal `baihaqybeki/sa7tein` (customer, merchant, courier, CS) dipakai sebagai acuan layar/alur saja — bukan target implementasi. Ringkasan ada di `repo-sa7tein-schema.md`. Portal CS-nya bisa jadi acuan bentuk Super Admin, tapi nama & shell mengikuti keputusan repo ini.
- **Design system di luar cakupan** dokumen ini (diputuskan PO 2026-09-21).

## Item di luar milestone (terbuka)

- **Model ongkir — SELESAI (2026-09-23).** PRD v2 memutuskan zona **Hijazi/Syimali ≤2 km** + merchant mengaktifkan zona (`C-13`), ongkir **100% merchant** (`C-07`) lewat `merchantDeliveryConfig`. Kode sudah dimigrasikan dari pita radius A/B/C (legacy) ke model ini; dokumentasi di `/documentation` bagian 32. **Nominal ongkir final tetap `UNRESOLVED`** (flow `f20-address-zone`), jadi angka di kode adalah placeholder — bukan tarif aktif.
- **Cakupan Super Admin — BUTUH KEPUTUSAN.** Konsol website penuh non-PWA; prefix `/superadmin` disiapkan tanpa route (lihat `decision-irbid-mvp.md`, DEC-2003).
- **Panel CS menampilkan IDR + JOD (opsional).** Konsol sekarang JOD-only; helper kurs sudah satu sumber di `src/data/currency.ts`, jadi tinggal keputusan tampilan.
- **Terkunci `UNRESOLVED` — jangan diisi dengan tebakan:** OQ-22 fee payout (M3, placeholder), OQ-26/OQ-28 provider & umur fallback kurs (M1), OQ-29 kategori + window dispute (M6), OQ-30 operator CS & pemilik konsol Super Admin, OQ-13 angka SLA (M5), OQ-14 penalti customer (M5), sign-off konsultan pajak (M7), validasi push di device (M8), insentif I-3..I-6 (M10).
