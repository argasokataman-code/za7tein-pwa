# Audit 003 — Hasil Perbaikan · 2026-09-24

Tindak lanjut `audit-003-2026-09-24.md`. Semua 8 temuan dikerjakan. Angka di bawah hasil
pengukuran DOM pada build `localhost:4173`, `/merchant`, viewport 390×844 @2 dan 1440×900.

Panduan desain: NeedMCP craft floor + playbook `operate` dan `layout` (mode product UI).

---

## Keputusan yang membalik #3

Temuan #3 ("dua chart mini berdampingan = layout dipaksakan") **dibatalkan**: layout dua kolom itu
justru penyebab #1 dan #2. Chart kembali **penuh lebar**. Ini keputusan sadar, bukan kompromi:
kartu tren naik dari 223px ke 371px, tapi nilai dan label hari akhirnya muat.

## Hasil terukur

| # | Temuan | Sesudah | Bukti |
|---|---|---|---|
| 1 | Nilai tumpang tindih 1,3px × 5 pasang | **0 tumpang tindih** | kolom 11,1px → **34,9px**; teks "44rb" 24,4px muat |
| 2 | Label hari menyatu ("RabKamJumSab") | **0 tumpang tindih** | lebar sama 34,9px, label hari muat |
| 3 | Chart mini berdampingan | **penuh lebar, varian compact** | bar width 150px → **316px** (390px) / **356px** (1440px) |
| 4 | Judul "Komposisi hari ini" tanpa data tanggal | **"Komposisi order"** | `judulKomposisi` = `Komposisi order` |
| 5 | Kartu komposisi 468px | **395px** (−73px) | donut kedua dibuang → `donutCount` 2 → **1** |
| 6 | Donut COD/transfer 50/50 | **bar perbandingan 2 warna** | `segW` 158/158, track 10px, legenda 13px, `aria-label` "4 order COD, 4 order transfer" |
| 7 | Batang 0 digambar batang 3px | **garis dasar 2px** | `fills(px)` = `[0,28,28,28,28,56,56]`; Sen = **0px** (bukan batang kecil) |
| 8 | Satuan dijelaskan dua kali | **caption "Pendapatan per hari"** | "(ribuan rupiah)" dibuang; "rb" sudah menjelaskan |

## Tinggi halaman

2141px → **2215px** (+74). Naik, dan itu benar: keterbacaan grafik pulih, kartu komposisi turun
73px sebagai gantinya. Craft floor: *"a clean scan cannot prove hierarchy"* — tinggi bukan ukuran
ukur yang lebih tinggi dari keterbacaan.

## Verifikasi

- `npm run lint` 0 error, `npm run build` bersih (192 file).
- `overflowX` **0** di 390px dan 1440px.
- Nol crash, nol console error.
- `browser-gate --route /merchant --route /merchant/insentif --pwa --strict` → **PASS 4/4**
  (390 & 1440).
- Angka tetap dari sumber yang sama: tren 8 order = `merchantOrders.length`; Rp256.000 = total
  order berjalan tanpa `ditolak`; donut 2/2/2/1/1 = 8; COD/transfer 4/4 = 8.

## Gap jujur (belum terverifikasi)

- **Pembacaan `aria-label` oleh pembaca layar nyata** belum diuji; yang terverifikasi hanya string
  `aria-label` ada dan benar (`role="img"` pada `.merchant-duo`).
- **NeedMCP tools tidak ke-load sebagai tool MCP di sesi ini** (config `needmcp` ada, endpoint
  `https://needmcp.com/mcp` hidup dan menjawab `tools/list` via HTTP). Panduannya diambil lewat
  HTTP langsung, bukan lewat tool MCP. Kalau mau dipakai sebagai tool, restart sesi.
