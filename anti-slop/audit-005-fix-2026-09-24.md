# Audit 005 Fix — Checkout customer · 2026-09-24

Semua temuan `anti-slop/audit-005-2026-09-24.md` (10/10) dikerjakan. Angka sebelum dari
audit, angka sesudah diukur ulang di DOM, viewport **390×844 @2** dan **1440×900**.

| # | Temuan | Sebelum | Sesudah (terukur) |
|---|---|---|---|
| 1 | Gate full-bleed | `.wallet-gate` `x=0 w=390` (390), `x=505 w=430` (1440) | Gate pindah ke dalam `.checkout-content` (JSX): `@20,920 350×154` di 390 — gutter `--space-5` seperti kartu lain |
| 2 | Hierarki datar | 6 peran semuanya 14,34px/600 | Judul seksi `13px/700` `--text-secondary`, isi (nama toko, nama item) `16px/600`, nominal item `16/700`, total `16,73/800` → empat tingkat 16,73 / 16 / 13 / 12 |
| 3 | Huruf < lantai mobile | `--text-base` 14,34px, `--text-xs` 11,56px di 390px | Token root: `--text-base: 16px`, `--text-xs: 12px`. Terukur: nama item & nominal 16px, catatan pajak 12px (tinggi catatan 60→62px). Lebar ≥667px tidak berubah (angka atas clamp lama sudah 16/12 di situ) |
| 4 | Dua sistem kartu | item `#EDE4DC`/radius 10 vs ringkasan `#DFD2C6`/12 | item kini `border rgb(223,210,198)` = `--border-strong`, `radius 12px` = `--radius-lg`, identik dengan kartu ringkasan |
| 5 | Top-up 4× | judul gate + isi gate + link + label tombol | Judul gate dihapus, isi jadi satu paragraf (saldo + minimal top-up + cakupan gate). Penyebutan kalimat "Top-up dulu" tinggal di label tombol nonaktif; gate body 12px, gate `350×154` |
| 6 | Pasangan uang 12× | kolom kanan item 141px, nama item 107px | Baris ringkasan + item + tombol `moneyPlain`; pasangan `Rp… · ±JOD` tinggal di Total Bayar, catatan kurs, dan gate. Terukur: total item `70×24` (dari 141), nama item `124px` (dari 107) |
| 7 | Casing | `Ringkasan Pesanan` | `Ringkasan pesanan` |
| 8 | Hex mentah legacy | `#FFF9F4 #EDE4DC #202020 #FFFFFF #6B6865 #F15A37 #FBE9E2`, margin 9px/80px | Aturan checkout di `app/part-09/10/17.scss` memakai `var(--bg-warm|--border|--text-primary|--surface|--text-secondary|--sa7tein-orange|--orange-soft|--on-brand)`, `margin-bottom: var(--space-5)`, `margin: 0 0 var(--space-2)`. Nilai identik → nol perubahan visual |
| 9 | Hover desktop | `.proceed-btn:hover { transform: translateY(-2px) }` | Dihapus bersama `:active`-nya; transisi tetap dari mixin `s-btn-shape` |
| 10 | CTA beda bentuk | empty 260×44 vs terisi 350×44 | `.checkout-empty-state` padding-inline `--space-5` + buang `max-width: 260px` → empty `@20,503 350×44`, identik dengan CTA terisi |

Berkas: `src/styles/_tokens.scss`, `src/styles/system/_checkout.scss` (baru),
`src/styles/system/_cart.scss`, `src/styles/_system.scss`,
`src/styles/app/part-09|10|17.scss`, `src/pages/Checkout.tsx`,
`src/components/ui/WalletTopUpGate.tsx`, dokumentasi `AppPolishSection` + versi v2.69.0.

## Verifikasi

- `./scripts/verify.sh` (oxlint + `tsc -b` + build): **0 error**
- `npm run scan`: **0 FAIL**, 1 WARN lama (`wallet-gate__text` dsb., tidak berubah)
- `npm run governance:check`: **OK** (`irbid-mvp-v2-2026-09-21`, 86 token)
- `browser-gate --route /customer/checkout --strict --click`: **2/2 PASS** (390 & 1440),
  klik nyata **5/5** hit-test (1 kontrol nonaktif dilewati)
- `browser-gate --role customer --strict`: **70/86** — **set FAIL identik dengan baseline
  bersih (git stash, diukur ulang)**: 8 rute sama (`home`, `search`, `filter`, `menu-detail`,
  `payment-amount`, `order-placed`, `wallet/top-up`, `payment-account`), semuanya target legacy
  `< 44px` (contoh "Tambah Sate Ayam" 91×33 di baseline, 93×34 sesudah — gagal di keduanya).
  **Nol FAIL baru dari perubahan ini.**
- `browser-gate --role merchant --strict`: **24/24** · `courier`: **10/10** · `admin`: **12/12**
- `/documentation`: render, badge **v2.69.0**, baris "Checkout customer (audit 005, 10 temuan)"
  ada di section 37, tanpa crash (assert `SECTIONS.length` lolos)

## Catatan

- Token `--text-base`/`--text-xs` adalah perbaikan **root** (pola preceden `--text-field` audit
  004): seluruh repo ikut, jadi setiap role ikut digate — di atas.
- Pasangan uang tinggal di Total Bayar + catatan kurs + gate: R-CURR-01 tetap terpenuhi
  ("kalimat wajib dekat nominal JOD" ada di catatan kurs dan total). Kalau product minta pasangan
  di tiap baris, itu keputusan terpisah.
- Temuan #5 sengaja menyisakan label tombol "Top-up dulu · saldo kurang": alasan tombol nonaktif
  wajib hidup di labelnya (DNA pola 1).
