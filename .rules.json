{
  "version": 2,
  "description": "Sa7tein PWA — front-end showcase. Product authority: docs/product/prd/manifest.json; UI contract: docs/design/DNA.md.",
  "productAuthority": {
    "manifest": "docs/product/prd/manifest.json",
    "intake": "docs/product/prd/README.md",
    "activeRevision": "irbid-mvp-v2-2026-09-21",
    "rule": "PDF/BRS baru proposed sampai source-backed analysis, decision record, dan milestone plan selesai; jangan menyimpulkan dari nama/versi file",
    "flows": "docs/design/flows/INDEX.json",
    "flowsPlan": "docs/design/flows/PLAN.md",
    "flowsRule": "Perilaku wajib ikut PRD aktif DAN flow yang sudah ada. Kalau perubahan menyimpang dari flow, tandai UNRESOLVED (dengan sumber) dan update flow + PRD di commit yang sama. Regenerasi diagram: ./scripts/flows-gate.sh <slug>. Dienforce di governance:check (paritas folder<->INDEX, spec+README ada, terikat revisi PRD aktif)"
  },
  "designDNA": "docs/design/DNA.md",
  "designDebt": "docs/design/legacy-debt.json (inline SVG count per TSX file may only decrease without a recorded decorative exception)",
  "scope": {
    "is": ["UI/UX", "design system", "mock data di src/data", "Redux state"],
    "is_not": ["backend/API/database", "autentikasi sungguhan", "pembayaran sungguhan", "Web Push service worker"]
  },
  "agent": {
    "startup_hooks": [
      "baca manifest PRD aktif dan docs/design/DNA.md dulu",
      "baca docs/design/flows/INDEX.json + flow relevan sebelum mengubah perilaku",
      "baca .rules.json dulu",
      "baca AGENTS.md",
      "baca .githooks/pre-commit",
      "atlas query topik sebelum usul perubahan"
    ]
  },
  "stack": {
    "build": "Vite + TypeScript",
    "ui": "React 19 + react-router-dom v7",
    "styling": "SCSS, bundel di src/styles/ (_system.scss = lapisan yang menang)",
    "state": "Redux Toolkit + redux-persist (cart, favorites, accountSetup)",
    "form": "react-hook-form + zod",
    "icons": "lucide-react saja (strokeWidth eksplisit)",
    "map": "leaflet + react-leaflet",
    "pwa": "vite-plugin-pwa (service worker hanya di build)"
  },
  "commands": {
    "dev": "npm run dev",
    "build": "npm run build",
    "lint": "npm run lint",
    "preview": "npm run preview",
    "browser:gate": "node scripts/browser-gate.mjs",
    "flows:gate": "./scripts/flows-gate.sh"
  },
  "browserTesting": {
    "wajib": "Setiap fix atau fitur yang menyentuh UI wajib lewat gerbang browser sebelum commit. Tidak ada 'sudah kelihatan benar'.",
    "perintah": {
      "satu_role": "node scripts/browser-gate.mjs --role customer",
      "halaman_disentuh": "node scripts/browser-gate.mjs --route /home --strict",
      "klik_nyata": "node scripts/browser-gate.mjs --route /home --strict --click",
      "semua_role": "node scripts/browser-gate.mjs --role all"
    },
    "syarat": [
      "npm run dev jalan di http://localhost:5173",
      "klon brave-debug hidup; skrip menyalakannya sendiri kalau perlu"
    ],
    "angka_dari_kode": "Pembanding dibaca dari src/styles/_tokens.scss (shell-max, touch-min, space-5) dan invarian di layout_invariants. Nilai runtime berbeda dari sumber = bug, bukan selera. Gate juga membuktikan viewport emulasi benar-benar berlaku sebelum angkanya dipakai.",
    "aturan_klik": "Klik HARUS lewat input nyata yang di-hit-test di titik tengah elemen (brave-debug.click, atau Input.dispatchMouseEvent di scripts/browser-gate.mjs). Gate melaporkan OVERLAY kalau titik tengahnya tertutup elemen lain. DILARANG menilai klik dari eval(\"el.click()\") atau dispatchEvent: keduanya menembus lapisan apa pun dan tidak membuktikan apa pun.",
    "viewport": "Selalu ukur pada emulasi 390x844 @2 dan 1440x900 — jangan dari jendela klon apa adanya (pernah 500x600 dan angkanya tidak sah).",
    "severity": {
      "fail": ["overflow-x != 0", "scroller bersarang di dalam halaman", "lebar kolom != min(viewport, --shell-max)", "token runtime != sumber", "viewport != emulasi", "halaman crash / kosong", "klik kena OVERLAY atau memicu exception"],
      "warn": ["target < --touch-min 44px", "gutter != --space-5 20px", "bilah fixed keluar kolom"],
      "strict": "--strict mempromosikan WARN jadi FAIL; wajib dipakai di halaman yang disentuh"
    },
    "bukti": "Tempel keluaran skrip (per-rute PASS/FAIL + angka) di laporan akhir. Sebut angka, bukan kesan."
  },
  "layout_invariants": {
    "shell_max": "430px (kolom aplikasi terkunci, mobile-first)",
    "page_gutter": "--space-5 (20px), tanpa pengecualian",
    "scroll": "satu penggulung: dokumen. Dilarang overflow-y: auto bersarang",
    "fixed_bars": "wajib max-width: var(--shell-max); margin-inline: auto",
    "sticky_header": "margin-inline: calc(-1 * var(--space-5)); padding-inline: var(--space-5)",
    "media_query": "merespons lebar JENDELA, bukan kolom. Untuk kolom sempit pakai container query / layout ringkas tanpa syarat"
  },
  "max_lines": {
    "component_tsx": 600,
    "page_tsx": 700,
    "page_exempt": ["Documentation.tsx (layar docs, berdiri sendiri)"],
    "hook": 150,
    "data": 500,
    "style_scss": 600,
    "context": "co-located .css hanya untuk komponen besar yang berdiri sendiri (kini CustomerHomeHero.css)"
  },
  "forbidden_patterns": {
    "code": [
      "dangerouslySetInnerHTML",
      "eval(",
      "new Function(",
      "innerHTML =",
      "console.log (di src/)",
      "useAppStore( (hook tidak ada; pakai useAppDispatch + useAppSelector)"
    ],
    "packages": ["react-icons", "@heroicons", "@fortawesome", "@tabler/icons", "emoji di UI atau kode"],
    "values": [
      "angka/string dipakai >1 tempat sebagai literal di JSX (wajib export const di src/data/)",
      "nilai warna di luar peran token (lihat AGENTS.md §4)",
      "var(--...) yang tokennya belum ada",
      "URL gambar eksternal (aset dari public/assets/)"
    ],
    "svg": ["preserveAspectRatio=\"none\" (pakai xMidYMid slice)"]
  },
  "reusable": {
    "order": ["OrderStageScreen", "Journey Line / status pesanan (src/components/OrderStageScreen.tsx)"],
    "ui": ["FoodCard", "AddToCartButton", "FavoriteButton", "BackButton"],
    "layout": ["BottomNav", "HomeIndicator"],
    "customer": ["CustomerHomeHero"]
  },
  "code_style": {
    "file_naming": "PascalCase",
    "components_ui": "named export",
    "pages": "default export",
    "props": "interface XxxProps di atas komponen (atau inline type)",
    "slice": "createSlice, name camelCase = key combineReducers, default export reducer + named actions",
    "hooks": "src/hooks/, useXxx, named export, return objek polos (bukan array)",
    "route": "tuple [path, Page] di array routes (src/App.tsx), bukan <Route> terpisah, tanpa lazy import",
    "types": "src/types.ts satu-satunya sumber tipe domain; import type { X } from '../types'"
  },
  "git": {
    "commitStyle": "Conventional Commits",
    "hooksPath": ".githooks",
    "bypass": "git commit --no-verify (darurat saja, wajib lapor)"
  },
  "pre_commit": {
    "checks": ["forbidden patterns", "line counts", "lint (oxlint)", "build (tsc -b && vite build)", "PRD registry + flow specs + design tokens (governance:check)", "atlas check", "documentation sync"]
  },
  "documentation": {
    "global_page": "src/pages/Documentation.tsx",
    "rule": "Setiap perubahan src/ (pages|components|data|store|hooks) WAJIB ikut memperbarui halaman dokumentasi global di commit yang sama. Dienforce di .githooks/pre-commit cek #6.",
    "bypass": "git commit --no-verify (darurat, wajib lapor)"
  },
  "atlas": {
    "enabled": true,
    "cli": "$HOME/.config/opencode/skills/atlas-owner/scripts/atlas.mjs",
    "after_significant_work": "record >=1 node (type/status benar, conn >=1)"
  },
  "readBefore": ["AGENTS.md", ".githooks/pre-commit", "atlas query <topik>"]
}
